

import React from 'react';

export default function person({id, name, description, email, location, category, phone, review, address, createdAt, updatedAt  }) {
    return (
        <div>
            
        </div>
    )
}
